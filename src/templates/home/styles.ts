import styled from "styled-components";

export const Wrapper = styled.div`
  max-width: 1200px;
  margin-inline: auto;
  section {
    padding: 1rem;
    margin-bottom: 3rem;
  }
`;

export const Intro = styled.div`
  max-width: 800px;
  text-align: center;
  margin-inline: auto;
  margin-top: 4rem;
  margin-bottom: 4rem;
`;

